#map $http_upgrade $connection_upgrade {
#	default upgrade;
#	''      close;
#}
#
#upstream docker-filebot {
#	# If the reverse proxy server is not running on the same machine as the
#	# Docker container, use the IP of the Docker host here.
#	# Make sure to adjust the port according to how port 5800 of the
#	# container has been mapped on the host.
#	server 10.0.0.10:5800;
#}
#
#server {
#  listen 7461 ssl;
#  server_name home.example.com;
#  ssl_certificate /Certificate/certificate.pem;
#  ssl_certificate_key /Certificate/privateKey.key;
#
#	location /filebot/ {
#        auth_basic "Filebot Login";
#		auth_basic_user_file /etc/apache2/.htpasswd;
#		proxy_pass http://docker-filebot/;
#		location /filebot/websockify {
#			proxy_pass http://docker-filebot/websockify/;
#			proxy_http_version 1.1;
#			proxy_set_header Upgrade $http_upgrade;
#			proxy_set_header Connection $connection_upgrade;
#			proxy_read_timeout 86400;
#		}
#	}
#}
#